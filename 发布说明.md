# iCloud Privacy Mail 发布与使用说明

## 1. 这是什么

这是一个 Go 单文件后端项目，前端页面已经通过 `embed` 打进程序里。

解压后可以直接二开，也可以直接编译运行。

## 2. 目录说明

- `cmd/panel/main.go`：程序入口。
- `internal/app/`：后端、Apple/iCloud 协议、数据存储、页面模板。
- `internal/app/templates/`：前端页面模板。
- `config.example.json`：示例配置。
- `README.md`：完整功能说明。
- `iCloud隐私邮箱取码平台接入设计.md`：接口和设计记录。

源码包不包含真实数据：

- 不包含 `data/state.json`
- 不包含真实 `config.json`
- 不包含账号、Cookie、邮箱 API Key
- 不包含 `.git` 历史目录
- 不包含编译产物

## 3. Windows 本地运行

安装 Go 后，在项目目录执行：

```powershell
copy config.example.json config.json
go run ./cmd/panel --config config.json
```

默认打开：

```text
http://127.0.0.1:8787/
```

也可以编译成 exe：

```powershell
go build -trimpath -o icloud-privacy-mail.exe ./cmd/panel
.\icloud-privacy-mail.exe --config config.json
```

## 4. Linux 本地运行

```bash
cp config.example.json config.json
go run ./cmd/panel --config config.json
```

编译 Linux amd64：

```bash
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -ldflags="-s -w" -o icloud-privacy-mail ./cmd/panel
chmod +x icloud-privacy-mail
./icloud-privacy-mail --config config.json
```

## 5. 第一次管理员账号

系统没有默认管理员账号密码。

第一次打开页面后，自己注册账号：

- 第一个注册成功的账号会自动成为管理员。
- 后续注册账号是普通用户。

## 6. 配置文件

复制 `config.example.json` 为 `config.json` 后按需修改：

```json
{
  "host": "127.0.0.1",
  "port": 8787,
  "data_path": "data/state.json",
  "api_key": "",
  "public_base_url": "",
  "icloud_default_host": "www.icloud.com.cn",
  "icloud_client_id": "d39ba9916b7251055b22c7f910e2ea796ee65e98b2ddecea8f5dde8d9d1a815d"
}
```

关键字段：

- `host`：监听地址。本地建议 `127.0.0.1`。
- `port`：监听端口，默认 `8787`。
- `data_path`：数据文件路径。真实账号、登录态、邮箱和邮件缓存都在这里。
- `api_key`：全局自动取号 API Key，可留空。
- `public_base_url`：复制邮箱 API 时使用的外部域名，例如 `https://www.example.com`。
- `icloud_default_host`：Apple/iCloud 默认域，国内账号常用 `www.icloud.com.cn`，海外账号可用 `www.icloud.com`。

## 7. 服务器部署示例

推荐目录：

```text
/opt/icloud-privacy-mail/
  icloud-privacy-mail
  config.json
  data/state.json
```

创建目录：

```bash
mkdir -p /opt/icloud-privacy-mail/data
cp icloud-privacy-mail /opt/icloud-privacy-mail/
cp config.example.json /opt/icloud-privacy-mail/config.json
chmod +x /opt/icloud-privacy-mail/icloud-privacy-mail
```

编辑 `/opt/icloud-privacy-mail/config.json`：

```json
{
  "host": "127.0.0.1",
  "port": 8787,
  "data_path": "/opt/icloud-privacy-mail/data/state.json",
  "api_key": "",
  "public_base_url": "https://你的域名",
  "icloud_default_host": "www.icloud.com.cn",
  "icloud_client_id": "d39ba9916b7251055b22c7f910e2ea796ee65e98b2ddecea8f5dde8d9d1a815d"
}
```

## 8. systemd 示例

创建 `/etc/systemd/system/icloud-privacy-mail.service`：

```ini
[Unit]
Description=iCloud Privacy Mail
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/icloud-privacy-mail
ExecStart=/opt/icloud-privacy-mail/icloud-privacy-mail --config /opt/icloud-privacy-mail/config.json
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

启动：

```bash
systemctl daemon-reload
systemctl enable --now icloud-privacy-mail
systemctl status icloud-privacy-mail
```

查看日志：

```bash
journalctl -u icloud-privacy-mail -f
```

## 9. Nginx 反代示例

```nginx
server {
    listen 80;
    server_name 你的域名;

    location / {
        proxy_pass http://127.0.0.1:8787;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

如果使用 HTTPS，请在 Nginx 或 Caddy 层配置证书。

## 10. 升级注意事项

升级程序时只替换二进制，不要覆盖数据文件：

```bash
systemctl stop icloud-privacy-mail
cp icloud-privacy-mail /opt/icloud-privacy-mail/icloud-privacy-mail
systemctl start icloud-privacy-mail
```

升级前建议备份：

```bash
cp /opt/icloud-privacy-mail/data/state.json /opt/icloud-privacy-mail/data/state.json.bak
```

不要把自己的 `state.json` 发给别人。

## 11. 当前线上服务器信息

> 记录时间：2026-06-29。这里保存的是连接、部署和回滚信息；不要把 SSH 密码、`config.json`、`state.json` 或任何 API Key 写进文档。

### 线上入口

- 站点域名：`https://www.kezong666.top/`
- Nginx 反代：`https://www.kezong666.top/` -> `http://127.0.0.1:8787`
- Nginx 配置：`/etc/nginx/sites-enabled/icloud-privacy-mail-www.kezong666.top.conf`
- 服务本机监听：`127.0.0.1:8787`

### SSH

```bash
ssh -p 2222 root@149.88.71.193
```

- 主机：`149.88.71.193`
- 端口：`2222`
- 用户：`root`
- 主机名：`ser117335700425`
- 架构：`linux/amd64` (`x86_64`)
- 密码：不写入仓库；需要时向服务器管理员索取。

### 服务与目录

- systemd 服务：`icloud-privacy-mail.service`
- 服务用户/组：`icloud-mail:icloud-mail`
- 项目根目录：`/opt/icloud-privacy-mail`
- 当前版本软链接：`/opt/icloud-privacy-mail/current`
- 发布目录：`/opt/icloud-privacy-mail/releases/<release-name>/`
- 临时上传目录：`/opt/icloud-privacy-mail/tmp/`
- 备份记录目录：`/opt/icloud-privacy-mail/backups/`
- 配置文件：`/opt/icloud-privacy-mail/shared/config.json`
- 数据文件：`/opt/icloud-privacy-mail/shared/data/state.json`

当前 systemd 启动命令：

```bash
/opt/icloud-privacy-mail/current/icloud-privacy-mail --config /opt/icloud-privacy-mail/shared/config.json
```

`/opt/icloud-privacy-mail/shared/` 是线上真实数据区，升级代码时严禁覆盖。

### 2026-06-29 已上线版本

- release：`20260629-admin-delete-users`
- 路径：`/opt/icloud-privacy-mail/releases/20260629-admin-delete-users`
- 上一版：`/opt/icloud-privacy-mail/releases/20260629-ui-categories-pagination-copyapi`
- 二进制 SHA256：`80d8d77b2264683640b42d013c58dade8a7bd8c4a241766250ebdbf9caba94c7`
- 验证结果：`/`、`/login`、`/manage` 公网均返回 `200`

本次上线内容：

- 首页改成左侧分类，只显示当前分类内容。
- 管理页也改成分类视图。
- 隐私邮箱数据页增加分页，默认每页 `10` 个，可自定义每页数量。
- 复制邮箱 API 的 TXT 格式为 `邮箱----API`。
- 管理员可以在用户列表删除普通用户账号；删除时会清理该账号的登录态、隐私邮箱和验证码邮件。

### 2026-06-30 本地已验证版本（待上线）

- 建议 release：`20260630-apple-account-manage-login`
- 本地提交说明：`Add Apple Account manage login flow`
- 站点域名仍为：`https://www.kezong666.top/`
- 服务和数据目录仍沿用本章记录；升级时只替换新 Linux 二进制，不覆盖 `/opt/icloud-privacy-mail/shared/`。

本次代码内容：

- 正式后端新增 Apple Account 新接口登录接口：
  - `POST /api/apple-account/login/start`
  - `POST /api/apple-account/login/2fa`
- 正式前端新增 `保存新接口登录态` 和 `提交新接口验证码` 按钮，不依赖 live 测试文件。
- Apple Account 新接口和旧 iCloud Web 登录态分别保存到 `login_states`，同一 Apple 账号的数据和邮箱 API 仍按原账号记录归属。
- 创建隐私邮箱时优先使用 Apple Account 新接口；没有新接口登录态时继续使用旧 iCloud Hide My Email 接口。
- 手动创建不再设置总数；当前勾选账号会在同一次请求里同时创建 1 轮，并在页面记录成功/失败数量。
- 定时创建默认间隔改为 60 分钟；每次定时可跑多轮，每轮勾选账号同时创建。某账号本次定时创建失败后只临时跳过该账号，其他账号继续，下一次定时创建重新尝试。
- Apple Account 请求链路已按 Roxy 浏览器实操修正：2FA 成功后保存 `/gs/ws/token` 返回的 `Set-Cookie`，再调用 `/account/manage` 获取动态 `apiKey`。
- 2FA 默认只走受信任设备验证码，不额外重发、不自动切短信。
- Apple Account 调试日志已脱敏，只输出请求摘要和指纹，不输出完整 Cookie、`scnt`、session、API key、账号邮箱、密码或验证码。

本地验证结果：

```powershell
go test ./...
go test -tags liveapple ./internal/app -run TestLiveAppleAccountManageLoginAndSave -count=1 -timeout 6m -v
go test -tags liveapple ./internal/app -run TestLiveAppleAccountCreateMailboxAndSave -count=1 -timeout 3m -v
```

验证结论：

- 单元测试全量通过。
- live 新接口登录已成功保存 Apple Account 登录态，`has_api_key=true`、`has_scnt=true`。
- live 新接口创建已成功，返回 `Origin=APPLE_ACCOUNT`，并已保存到本地邮箱记录。

### 日常检查命令

```bash
systemctl status icloud-privacy-mail.service --no-pager
journalctl -u icloud-privacy-mail.service -n 80 --no-pager
readlink -f /opt/icloud-privacy-mail/current
cd /opt/icloud-privacy-mail/current && sha256sum -c icloud-privacy-mail.sha256
curl -fsS http://127.0.0.1:8787/login >/dev/null
```

公网烟测：

```bash
curl -k -I https://www.kezong666.top/
curl -k -I https://www.kezong666.top/login
curl -k -I https://www.kezong666.top/manage
```

### 标准升级方式

本项目模板通过 `go:embed` 打进二进制，升级前端和后端都只需要替换 Linux 二进制。

本地构建：

```powershell
$env:CGO_ENABLED='0'
$env:GOOS='linux'
$env:GOARCH='amd64'
go build -trimpath -ldflags "-s -w" -o icloud-privacy-mail ./cmd/panel
```

远端升级原则：

1. 新建 `/opt/icloud-privacy-mail/releases/<release-name>/`。
2. 上传新二进制到该 release 目录并设置 `0755`。
3. 写入 `icloud-privacy-mail.sha256` 并执行 `sha256sum -c`。
4. 记录旧 `current` 指向到 `/opt/icloud-privacy-mail/backups/<release-name>-previous-release.txt`。
5. 原子切换 `/opt/icloud-privacy-mail/current` 软链接。
6. `systemctl restart icloud-privacy-mail.service`。
7. 先测 `http://127.0.0.1:8787/login`，再测公网域名。

不要上传或覆盖：

- `/opt/icloud-privacy-mail/shared/config.json`
- `/opt/icloud-privacy-mail/shared/data/state.json`
- `/opt/icloud-privacy-mail/shared/` 下任何真实运行数据

### 回滚方式

如果新版本服务启动失败或烟测失败，把 `current` 切回上一个 release 后重启：

```bash
old=/opt/icloud-privacy-mail/releases/20260629-ui-categories-pagination-copyapi
ln -sfn "$old" /opt/icloud-privacy-mail/current.next
mv -Tf /opt/icloud-privacy-mail/current.next /opt/icloud-privacy-mail/current
systemctl restart icloud-privacy-mail.service
systemctl status icloud-privacy-mail.service --no-pager
```

如果是后续版本，优先读取对应备份记录：

```bash
cat /opt/icloud-privacy-mail/backups/<release-name>-previous-release.txt
```

## 12. 常用操作

启动后进入首页：

```text
http://127.0.0.1:8787/
```

使用流程：

1. 注册第一个管理员账号。
2. 登录后台。
3. 在页面里保存 Apple/iCloud 登录态。
4. 创建或同步 Hide My Email 隐私邮箱。
5. 复制每个邮箱的 API 地址给其他项目取验证码。

对外取码 API 格式：

```text
http://你的域名/api/v1/mailboxes/邮箱地址/code?key=邮箱独立Key
```

如果短时间重新发送验证码，普通取码接口会跳过已经返回过的同一封旧验证码邮件；需要排查缓存时才加：

```text
&cache=1
```
